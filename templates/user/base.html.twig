<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Default Title{% endblock %}</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #1e1e2e;
            color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        header {
            margin-bottom: 20px;
        }

        .navbar {
            background: linear-gradient(135deg, #14172a, #1e2336);
            padding: 15px 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
        }

        .navbar-logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: #00aaff;
            text-decoration: none;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        .navbar-logo:hover {
            color: #66ccff;
            transform: scale(1.1);
        }

        .navbar-links {
            list-style: none;
            display: flex;
            gap: 20px;
            margin: 0;
            padding: 0;
        }

        .navbar-links li {
            display: inline;
        }

        .navbar-links a {
            text-decoration: none;
            font-size: 1rem;
            font-weight: bold;
            color: #d1d7e0;
            transition: color 0.3s ease, transform 0.3s ease;
            padding: 8px 15px;
            border-radius: 5px;
        }

        .navbar-links a:hover {
            color: #00aaff;
            background-color: rgba(0, 170, 255, 0.1);
            transform: translateY(-2px);
        }

        .language-switcher {
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .language-switcher form {
            display: inline;
        }

        .language-switcher button {
            background: #1e2336;
            color: #d1d7e0;
            border: 1px solid #34495e;
            border-radius: 5px;
            padding: 5px 10px;
            font-size: 0.9rem;
            cursor: pointer;
            transition: background 0.3s ease, color 0.3s ease;
        }

        .language-switcher button:hover {
            background: #00aaff;
            color: #fff;
        }

        footer {
            text-align: center;
            padding: 15px;
            background-color: #14172a;
            color: #d1d7e0;
            margin-top: 20px;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="navbar-container">
                <a href="/" class="navbar-logo">Hearthstone</a>
                <ul class="navbar-links">
                    <li><a href="{{ path('app_profile') }}">Profil</a></li>
                    <li><a href="{{ path('app_shop') }}">Boutique</a></li>
                </ul>
            </div>
        </nav>
    </header>
    <main>
        {% block body %}{% endblock %}
    </main>
    <footer>
        <p>&copy; Hearthstone</p>
    </footer>
</body>
</html>
